import { IOptionInitialiceEntry } from "@ptypes/positions/assisted/IOptionInitialiceEntry";
interface IRenderPersonal {
  values: IOptionInitialiceEntry[];
  isMobile: boolean;
}
export type { IRenderPersonal };
