import { sistemasenlinea } from "@design/tokens/tokensWithReference/sistemasEnlinea";
import { inube } from "@inubekit/inubekit";

type Theme = typeof sistemasenlinea & typeof inube;

export type { Theme };
