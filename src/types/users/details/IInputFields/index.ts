import { IField } from "@ptypes/interactiveModal/IField";
import { IPosition } from "@ptypes/positions/assisted/IPosition";

interface IInputFields {
  labels: IField[];
  infoData: IPosition;
}
export type { IInputFields };
